import React from 'react';
import {Button} from 'react-bootstrap';

class EmailAgent extends React.Component {
    render() {
        return(
            <Button style={{position:'absolute',right:'5%',bottom:'3%'}} variant="secondary">Email Agent</Button>
            );
    }
}
export default EmailAgent;